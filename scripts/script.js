const removeButton = document.querySelector(`.find-out-link`);
removeButton.remove();

const modifyPicWidth = document.querySelector(`.bio__pic`);
modifyPicWidth.style.width = "36rem";

const modifySkills = (skillsTitle, years) => {};
